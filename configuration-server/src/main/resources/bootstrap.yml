spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/webster91/config-repo
          search-paths: '{application}'
        encrypt:
          enabled: false
server:
  port: 5000
