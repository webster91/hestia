spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/webster91/hestia/config-repo/
          search-paths: ${spring.application.name}
        encrypt:
          enabled: false
server:
  port: 5000
encrypt:
  key: hestia-secret
